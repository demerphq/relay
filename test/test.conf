syslog_to_stderr=1
polling_interval_millisec=1
tcp_send_timeout_sec=2
server_socket_rcvbuf_bytes=33554432
sleep_after_disaster_millisec=1000

spill_usec=1000000
spillway_root=/tmp

graphite_send_interval_millisec=60000
graphite_sleep_poll_interval_millisec=500
graphite_arg=/tmp/relay.graphite
graphite_root=general.event_relay

# The first non-opt arg is where we listen.
# The rest (2nd, ...) are where we send.
udp@localhost:10000
tcp@localhost:9003
